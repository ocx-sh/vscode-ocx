# Everything is excluded except the bundled `dist/` output and the metadata
# files vsce includes by default (package.json, README, CHANGELOG, LICENSE).
.vscode/**
.vscode-test/**
.vscode-test.mjs
.github/**
.claude/**
.serena/**
src/**
out/**
node_modules/**

esbuild.js
eslint.config.mjs
tsconfig.json
.prettierrc
.npmrc
.gitignore
.editorconfig

AGENTS.md
CLAUDE.md
.mcp.json

# Repo dev tooling / strays — not part of the shipped extension.
ocx.toml
ocx.lock
.envrc
.pytest_cache/**

**/*.ts
**/*.map
**/.DS_Store
# SVG is repo branding only — Marketplace forbids SVG, and the PNG icon ships instead.
**/*.svg
