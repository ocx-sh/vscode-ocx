---
paths:
  # Globs that scope this tech rule. Match the file types / locations where
  # this technology is actually used in the extension. Examples:
  #   - "src/**/*.ts"            # everything (broad tech, e.g. the VS Code API)
  #   - "**/esbuild.*"           # build tooling
  #   - "src/**/*solr*.ts"       # a narrow integration surface
  - "[GLOB_PLACEHOLDER]"
---

# tech-[name] — [Technology] for the VS Code Extension

<!--
Per-technology rule. Generated by /add-tech-specialist.
Path-scoped: auto-loads only when an edited file matches the `paths:` globs above.
Register this file in .claude/rules.md (same commit) under "By technology".
Keep it concise — it is loaded into context on every matching edit.
Pair with the specialist-[name] agent, which carries this rule as its context.
-->

## Overview

[What this technology is, the version/range this rule targets, and why the
extension uses it. One short paragraph. Note the date the research was done so
future readers know when the API snapshot may have decayed.]

## Core model

[The mental model a contributor needs: key objects/APIs, lifecycle, the
request/response or activation flow, how it integrates with the extension
host. Keep to the essentials — link out for exhaustive reference.]

## Invariants

1. [Hard rule that must always hold — e.g. "register every disposable in
   `context.subscriptions`"]
2. [Invariant 2]
3. [Invariant 3]

## Gotchas

- [Footgun 1 — surprising behaviour, version skew, async ordering, etc.]
- [Gotcha 2]
- [Gotcha 3]

## Best practices

- [Practice 1]
- [Practice 2]
- [Practice 3]

## Sources

| Source | Type | Date | Relevance |
|--------|------|------|-----------|
| [URL] | [Official docs/Context7/Repo/Blog] | [YYYY-MM-DD] | [What it covers] |
